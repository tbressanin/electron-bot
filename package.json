{
  "name": "electron-bot",
  "version": "1.0.0",
  "description": "",
  "main": "out/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && cp src/index.html build",
    "start": "npm run compile && electron"
  },
  "author": "",
  "license": "ISC",
  "_id": "electron-bot@1.0.0",
  "devDependencies": {
    "electron": "13.1.2"
  }
}
